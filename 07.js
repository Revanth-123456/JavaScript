let isPremium=false;
let value=isPremium?"PremiumUser":"normal user";
console.log(value);


// const abc=()=>{ }
// we use arrow function when there is single line function
// it reduces number of lines and helpful to manage single line logic in single line statement instead of reg block;
// it will internally handle the keyword;

const sum = (a,b) =>{
    return a+b;
}

let value1=sum(5,10);
console.log('i have a value',value1);

const sum1 =(a,b) => a+b;



